apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: todo-view-claim0
  name: todo-view-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
